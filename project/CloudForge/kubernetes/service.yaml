apiVersion: v1
kind: Service
metadata:
  name: cloudforge-service
spec:
  type: NodePort
  selector:
    app: cloudforge
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
